# RegulaAI frontend

Interface React/TypeScript consommant exclusivement `/api/v1`.

```bash
npm install
npm run dev
```

Vérifications : `npm run lint`, `npm run typecheck`, `npm run test -- --run` et
`npm run build`. Vite relaie `/api` vers `http://127.0.0.1:8000`.
