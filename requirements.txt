# Tests
pytest==8.3.4

# Lightweight production retrieval over the committed legal index
numpy>=1.26.0
rank_bm25>=0.2.2

# Serveur MCP + CLI Inspector
mcp[cli]>=1.0.0

# Observabilite optionnelle (fallback local si non configure)
langfuse>=2.0.0

# LLM de raisonnement (DeepInfra, API compatible OpenAI) - optionnel, fallback
# deterministe si la cle DEEPINFRA_API_KEY est absente
openai>=1.0.0
python-dotenv>=1.0.0

# Adaptateur web (la CLI et le serveur MCP restent indépendants)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
httpx>=0.27.0,<1.0
