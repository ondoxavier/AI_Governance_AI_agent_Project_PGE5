# Tests
pytest==8.3.4

# Ingestion (PDF -> texte)
pymupdf>=1.24.0

# Retrieval hybride (BM25 + dense + reranking)
numpy>=1.26.0
rank_bm25>=0.2.2
sentence-transformers>=3.0.0

# Serveur MCP
mcp>=1.0.0
